# ข้อตกลงของโปรเจกต์นี้

## กฎสำหรับไฟล์ EA (`mt5/experts/*.mq5`)

### 1. แก้ไฟล์เมื่อไร ต้องอัปเวอร์ชันทุกครั้ง

ห้าม push การแก้ไข `.mq5` โดยไม่ขยับ `#property version` — ผู้ใช้ต้องดูออกทันที
ว่าไฟล์ในมือเป็นตัวล่าสุดหรือเปล่า โดยไม่ต้องเปิดอ่านโค้ด

| ประเภทการแก้ | ขยับเท่าไร |
|---|---|
| เปลี่ยนพฤติกรรม / เปลี่ยนค่าดีฟอลต์ / optimize | **+0.10** |
| แก้บั๊กล้วนๆ ไม่เปลี่ยนพฤติกรรมที่ตั้งใจไว้ | **+0.01** |

พร้อมกับเพิ่มบรรทัดใน changelog ที่หัวไฟล์เสมอ ระบุว่าเวอร์ชันนั้นเปลี่ยนอะไร

### 2. ผล optimize ต้องเป็นค่าเริ่มต้นเสมอ

เมื่อทดสอบแล้วพบว่าค่าไหนดีกว่า **ให้เขียนทับเป็นค่าดีฟอลต์ใน `input` เลย**
ห้ามเขียนไว้แค่ในคอมเมนต์หรือในรายงาน ผู้ใช้ต้องได้ค่าที่ดีที่สุดทันทีที่ลากอีเอ
ลงกราฟ โดยไม่ต้องมานั่งกรอกเอง

พร้อมกับเขียนคอมเมนต์กำกับตรงบรรทัดนั้นว่าตัวเลขมาจากไหน เช่น

```mq5
input double InpEntryDist = 0.30;   // ระยะคร่อมราคา (จูนแล้ว: 0.30)
```

**ข้อยกเว้น:** ถ้าผลทดสอบกระจัดกระจายจนไม่มีค่าไหนชนะชัด ให้ใช้ค่ากลางแล้ว
เขียนกำกับว่า "ค่ากลาง ไม่ fit" — ดีกว่าหยิบตัวเลขที่บังเอิญเด่นในตัวอย่างเล็กๆ

## หมายเหตุ

เครื่องที่พัฒนาเป็น Linux ไม่มี MetaEditor จึง **คอมไพล์ไม่ได้** ต้องแจ้งผู้ใช้
ทุกครั้งว่ายังไม่ผ่าน F7 และให้ตรวจ balance ของวงเล็บ/ปีกกาทางโปรแกรมแทน
