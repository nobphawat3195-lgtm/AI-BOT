[pytest]
testpaths = tests
asyncio_mode = auto
# A hung test must fail loudly rather than stall CI until the job limit.
# Every test here is offline and sub-second; 60s is pure headroom.
timeout = 60
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
